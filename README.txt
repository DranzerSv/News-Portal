Link to deployed project https://week-2-api.vercel.app/

In this project  I used The Guardian API in order to get news
and renderize them with an image, healine and url.

When an img does not exist or has a unsafe http:// direction,
it gets replaced with a default img.